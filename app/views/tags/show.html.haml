%section.py-5
  .container
    .pb-2
      %h3
        = "##{@tag.name}"

    %section.py-2
      %h5.text-muted= t('data_sets')
      .row
        - @data_sets.each do |data_set|
          .col-12
            = render data_set
