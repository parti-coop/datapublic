= simple_form_for @archive do |f|
  = f.input :slug
  = f.input :cover
  = f.input :title
  = f.input :body
  = f.input :publisher, as: "summernote"
  = f.submit data: { disable_with: "Please wait..." }, class: 'btn btn-primary'
